{% extends 'index.php.twig' %}


{% block content %}<div>
    <h1> {{user.login}}</h1>
    <h2> {{user.name}}</h2>
    <a href="/user/addToDB?id={{user.id}}"><button class="comeBackBtn">Обновить</button></a>
    <a href="/user/deleteFromDB&id={{user.id}}"><button class="comeBackBtn">Удалить</button></a>
</div>
{% endblock %}

